services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=true
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
